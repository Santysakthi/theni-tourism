# Server
PORT=4000

# Database (PostgreSQL)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=theni_tourism
DB_USER=your_db_user
DB_PASSWORD=your_db_password

# JWT (use a strong random string in production)
JWT_SECRET=your_jwt_secret_change_in_production
JWT_EXPIRES_IN=7d
